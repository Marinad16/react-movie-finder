import React from "react";
import HomeCarousel from "../components/Carousel/Carousel";

const HomePage = () => {

  return (
    <section>
        <HomeCarousel/>
    </section>
  );
};

export default HomePage;
